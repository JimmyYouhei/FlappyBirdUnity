using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FadeOutComplete : MonoBehaviour {
    // state to know if fade out complete to load next Scene
    public bool fadeOutComplete = false;
}
